import XCTest
@testable import HarvestBinCommandsCore

final class HarvestBinCommandsCoreTests: XCTestCase {
    func testExample() throws {
        let core = HarvestBinCommandsCore()
        XCTAssertNotNil(core)
    }
}