import XCTest
@testable import HarvestBinCommands

final class HarvestBinCommandsTests: XCTestCase {
    func testExample() throws {
        let commands = HarvestBinCommands()
        XCTAssertNotNil(commands)
    }
}